<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layoutFooter"
    android:layout_width="match_parent"
    android:layout_height="70dp"
    android:background="@color/main_footer_bg"
    android:visibility="gone"
    >

    <View
        android:id="@+id/viewSplit"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/main_footer_line" />

    <RadioGroup
        android:id="@+id/main_footer_radiogroup"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/main_footbar_home"
            style="@style/main_footer_bar_radio"
            android:checked="true"
            android:drawableTop="@drawable/main_footer_widget_bar_home_bg"
            android:text="@string/main_footer_home_text" />
        <RadioButton
            android:id="@+id/main_footbar_my"
            style="@style/main_footer_bar_radio"
            android:drawableTop="@drawable/main_footer_widget_bar_my_bg"
            android:text="@string/main_footer_my_text" />
    </RadioGroup>

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="@null"
        android:orientation="horizontal">

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

    </LinearLayout>
</RelativeLayout>